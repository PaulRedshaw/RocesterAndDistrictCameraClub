namespace ListPlus.Service.Configuration
{
    public class AutoMapperConfig
    {
        public static void Configure()
        {
            //Mapper.CreateMap<Listing, ListingModel>();
            //Mapper.CreateMap<ListingModel, Listing>();

            //Mapper.CreateMap<ListingItem, ListingItemModel>()
            //    .ForMember(m => m.Priority, opt => opt.MapFrom(s => (Priority?)s.Priority));
            //Mapper.CreateMap<ListingItemModel, ListingItem>()
            //    .ForMember(m => m.Priority, opt => opt.MapFrom(s => (int?)s.Priority));

            //Mapper.CreateMap<LookAndFeel, LookAndFeelModel>();
            //Mapper.CreateMap<LookAndFeelModel, LookAndFeel>();
        }
    }
}
